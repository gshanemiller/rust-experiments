[package]
name = "log"
version = "0.1.0"
edition = "2024"

[features]
debugAllocator = []
debugAllocatorStats = []
debugAllocatorTrace = []

[dependencies]
log = {version="0.4", features = ["max_level_error", "release_max_level_warn"]}
