[package]
name = "alloc"
version = "0.1.0"
edition = "2024"

[features]
debugAllocatorStats = []
debugAllocatorTrace = []
default = ["debugAllocatorStats" , "debugAllocatorTrace"]

[dependencies]
libc = "0.2"
